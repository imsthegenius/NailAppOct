import { gradients, themeVariants } from '../src/theme/tokens';

export const onboardingGradients = gradients.onboarding;
export const surfaceGradients = gradients.surfaces;
export const screenGradients = gradients.screens;

export const overlayShapes = themeVariants.light.overlay;
